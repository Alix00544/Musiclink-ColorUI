<cu-custom bgColor="bg-white" isBack="{{true}}">
	<view slot="content">排行榜</view>
</cu-custom>
<!-- 选择栏 -->
<scroll-view scroll-x class="bg-white nav text-center" scroll-with-animation scroll-left="{{scrollLeft}}">
	<view class="cu-item {{index==TabCur?'text-red cur text-bold':'text-gray'}} text-lg" wx:for="{{ranklist}}" wx:key="key" bindtap="tabSelect" data-id="{{index}}">
		{{item.rankname}}
	</view>
</scroll-view>

<!--内容主体-->
<swiper current="{{TabCur}}" duration="200" bindchange="swiperChange" style='height:{{WinHeight-CustomBar-45}}px'>
	<swiper-item class='bg-white' wx:for="{{ranklist}}" wx:key="key">
		<scroll-view bindscrolltoupper="upper" scroll-y style="height: 100%;">
			<!-- 榜单列表 -->
			<view class="cu-list menu-avatar">
				<view class="cu-item" wx:for="{{item.songlist}}" wx:key="key" wx:for-item="song">
					<view class="cu-avatar radius lg" style="background-image:url({{song.coverImg}});width:120rpx;height:120rpx;opacity:0.8;font-size: 3em;">{{index+1}}</view>
					<view class="content" style="width:calc(100% - 120rpx - 60rpx - 250rpx - 40rpx);left:170rpx">
						<text class="text-cut text-bold text-lg">{{song.name}}</text>
						<view class="text-gray text-sm flex">
							<text class="text-cut">{{song.singer}}</text>
						</view>
					</view>
					<view class="action" style="width:250rpx" bindtap="bindSing" data-song="{{song.name}}">
						<button class="cu-btn bg-red round text-center text-sm" style="width:120rpx;height:50rpx">唱歌</button>
					</view>
				</view>
			</view>
			<!-- 底部提示 -->
			<view class="text-center text-gray padding">————
				<text class="padding-lr">我是有底线的</text>
				————
			</view>
		</scroll-view>
	</swiper-item>
</swiper>